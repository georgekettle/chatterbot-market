<%# update contents of modal turbo frame tag with message_modal partial %>
<% if @feedback.persisted? %>
  <%= turbo_stream.update :turbo_modal, partial: "message_modal", locals: { feedback: @feedback } %>
<% end %>