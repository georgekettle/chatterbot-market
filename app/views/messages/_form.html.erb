<%= turbo_frame_tag dom_id(conversation, :message_form) do %>
  <%= form_with(model: message, url: conversation_messages_path(conversation), data: { turbo_target: '_top' }) do |form| %>
    <% if message.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(message.errors.count, "error") %> prohibited this message from being sent:</h2>

        <ul>
          <% message.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="ring-1 ring-tertiary-300 rounded shadow flex items-center focus-within:ring-primary-500 focus-within:ring-2 overflow-hidden">
      <%= form.text_field :content, placeholder: "Write a message for #{conversation.chatbot.name}...", class: 'border-0 ring-0 focus:ring-0 focus:outline-none' %>
      <div class="p-0.5">
        <%= form.button type: :submit, class: 'btn btn-primary p-2 bg-primary-600 hover:bg-primary-700' do %>
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5">
            <path d="M3.478 2.405a.75.75 0 00-.926.94l2.432 7.905H13.5a.75.75 0 010 1.5H4.984l-2.432 7.905a.75.75 0 00.926.94 60.519 60.519 0 0018.445-8.986.75.75 0 000-1.218A60.517 60.517 0 003.478 2.405z" />
          </svg>
          <span class="sr-only">Send</span>
        <% end %>
      </div>
    </div>
  <% end %>
<% end %>